package br.sisacademico.utilidade;

public enum AcaoDao {
    CAD,     //C (CREATE)
    LER,      //R (READ)
    EDI,       //U (UPDATE)
    EXC,     //D (DELETE)
    CARREGA,
    CADASTRO,
    EDICAO;
}
